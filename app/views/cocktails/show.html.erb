<h1><%= @cocktail.name %></h1>

<% @cocktail.doses.each do |dose| %>
  <p><%= "#{dose.description} #{dose.ingredient.name}" %></p>
<% end %>

<%= link_to "Add dose", new_cocktail_dose_path(@cocktail) %>
<p><%= link_to "Main Page", cocktails_path  %></p>
